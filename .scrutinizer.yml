checks:
  php: true

filter:
  paths: ['src/*']

build:
  environment:
    php:
      version: 7.0
  tests:
    before:
      - "cp composer.lock.example composer.lock"
      - "composer install --dev"
