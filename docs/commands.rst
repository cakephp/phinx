.. index::
   single: Commands
   
Commands
========

Phinx is run using a number of commands.

The Create Command
------------------

The Init Command
----------------

The Migrate Command
-------------------

The Rollback Command
--------------------

The Status Command
------------------